#include <stdio.h>

/**
 * main - entry point
 * body of code
 * Return: Always 0 if successful
 */

int main(void)

{
	int r;

	for (r = 0; r <= 9; r++)
		printf("%i", r);
	printf("\n");

	return (0);
}
