#include <stdio.h>

/**
 * main - entry point
 * code body
 * Return: Always 0 if successful
 */

int main(void)

{
	int r = 0;

	for (; r < 10; r++)
		putchar('0' + r);
	putchar('\n');

	return (0);
}
